import { Database } from '@/types/supabase';

export type TemplateChannelType = Database['public']['Enums']['channel_type'];

export type TemplateStatusType = Database['public']['Enums']['status_type'];
